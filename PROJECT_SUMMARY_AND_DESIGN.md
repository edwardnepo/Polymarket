# סיכום ותכנון הפרויקט

## 1. כותרת הפרויקט

**ניתוח הקשר בין סנטימנט חדשותי לבין תנועות הסתברות בשווקי Polymarket**

## 2. שאלת המחקר המעודכנת

**האם סנטימנט בכתבות חדשותיות קשור לתנועות הסתברות בשווקי Polymarket, והאם ניתן להשתמש בו לחיזוי תנועה עתידית?**

## 3. שינוי היקף הפרויקט

בתוכנית המקורית הוזכר שימוש ב-Twitter/X וב-Reddit כמקורות מידע חברתיים. באב-הטיפוס הנוכחי מקורות אלה **אינם ממומשים**. היקף הפרויקט צומצם באופן מכוון ל:

- כתבות חדשותיות ממקורות RSS.
- נתוני שווקים והיסטוריית מחירים מ-Polymarket.
- ניתוח סטטיסטי ומודל חיזוי על בסיס חדשות + שוק בלבד.

הצמצום מאפשר לבנות אב-טיפוס עובד, יציב ובר-הרצה, ללא תלות בממשקי API חברתיים מוגבלים או בתנאי שימוש משתנים.

## 4. מטרת המערכת

המערכת אוספת כתבות חדשותיות בנושאי גיאופוליטיקה וביטחון, מנתחת את הסנטימנט שלהן באמצעות מודל NLP, מקשרת אותן לשווקי Polymarket רלוונטיים, מבצעת ניתוח מתאם סטטיסטי, בונה דאטהסט למידת מכונה, מאמנת מודל לחיזוי תנועת הסתברות עתידית, ומציגה את התוצאות בלוח מחוונים אינטראקטיבי ב-Streamlit.

המודל אינו מנסה לנבא את המציאות עצמה. הוא מנסה לנבא תנועה עתידית בהסתברות המשתמעת בשוקי Polymarket.

## 5. ארכיטקטורת המערכת

### שכבת איסוף נתוני חדשות

`data/news_scraper.py` קורא עדכוני RSS ממקורות חדשות בינלאומיים וישראליים, מסנן כתבות לפי מילות מפתח גיאופוליטיות, שומר את זהות מקור התקשורת, ומחזיר רשומות כתבה מובנות.

### שכבת NLP / סנטימנט

`ml/sentiment_analyzer.py` משתמש במודל Hugging Face כדי להפיק ציון סנטימנט חתום לכל כתבה. בפרויקט הציון מוצג כמדד מתיחות ביטחונית: ערך שלילי מצביע על סיקור של הסלמה ולחימה, וערך חיובי מצביע על רגיעה ודיפלומטיה.

### שכבת נתוני Polymarket

`data/polymarket_client.py` אוסף שווקים פעילים ורלוונטיים מ-Polymarket, מסנן נושאים לא רלוונטיים, ושומר היסטוריית הסתברות של טוקן “Yes”.

### שכבת ניתוח סטטיסטי

`pipeline/run_pipeline.py` מחשב מתאם Pearson בין סנטימנט חדשותי לבין הסתברויות שוק. ניתוח זה נשמר כניתוח סטטיסטי משלים ואינו מוצג כראיה סיבתית.

### שכבת Machine Learning

`ml/forecasting.py` בונה דאטהסט שבו כל תצפית מקושרת לשוק ולזמן. המאפיינים כוללים:

- הסתברות היסטורית בשוק.
- סנטימנט כתבות.
- נפח כתבות.
- מקור תקשורת כמאפיין.
- חלונות זמן של 1, 3, 6, 12 ו-24 שעות.
- נושאי תוכן כגון Iran, Israel, Gaza, Hamas, Hezbollah, ceasefire, sanctions ועוד.

המודל משווה Random Forest מול תחזיות בסיס:

- ללא שינוי: delta = 0.
- שינוי קודם שנצפה.
- דלתא היסטורית ממוצעת.

הערכת המודל נעשית באמצעות חלוקת זמן: תצפיות מוקדמות לאימון ותצפיות מאוחרות לבדיקה.

### שכבת Dashboard / Frontend

`frontend/app.py` מציג לוח מחוונים בעברית. המסך הראשי מציג סיכום מנהלים, סיכום תוצאות אוטומטי, ניתוח מקורות תקשורת, תחזית עתידית וניתוח מתקדם בתוך אזור נפתח.

### שכבת אחסון

המערכת יכולה לעבוד עם Firebase Firestore, אך לצורך הדגמה למורה היא יכולה לעבוד ללא Firebase באמצעות קבצי snapshot מקומיים:

- `output/articles.json`
- `output/markets.json`
- `output/run.json`
- `output/ml_results.json`
- `output/ml_dataset.json`

## 6. קבצים מרכזיים

- `config/settings.py` — הגדרות מרכזיות: מקורות RSS, מילות מפתח, Firestore, Polymarket, חלון lookback והגדרות pipeline.
- `data/news_scraper.py` — איסוף כתבות RSS, סינון לפי מילות מפתח ושמירת מקור התקשורת.
- `data/polymarket_client.py` — איסוף שווקים והיסטוריית הסתברויות מ-Polymarket.
- `data/firebase_client.py` — שכבת גישה ל-Firestore, אופציונלית להפעלת דמו.
- `ml/sentiment_analyzer.py` — ניתוח סנטימנט כתבות באמצעות Hugging Face.
- `ml/forecasting.py` — בניית דאטהסט ML, יצירת מאפייני lag/topic/source, אימון Random Forest, השוואה ל-baseline, דיוק כיווני ותווית אמינות.
- `pipeline/run_pipeline.py` — הרצת כל השלבים מקצה לקצה וכתיבת snapshot מקומי.
- `frontend/app.py` — לוח מחוונים Streamlit.
- `output/*.json` — קבצי דמו מקומיים שמאפשרים להריץ את הדשבורד ללא Firebase.
- `tests/test_forecasting.py` — בדיקות יחידה לשכבת החיזוי.

## 7. מה כבר ממומש

- איסוף כתבות RSS.
- מקורות תקשורת ישראליים כגון Ynet, Maariv ו-Globes, לצד מקורות בינלאומיים.
- ניתוח סנטימנט.
- איסוף שווקי Polymarket והיסטוריית הסתברויות.
- מתאם Pearson.
- דאטהסט Machine Learning.
- מודל Random Forest לחיזוי תנועת הסתברות.
- השוואה מול תחזיות בסיס.
- דיוק כיווני.
- ניתוח לפי מקור תקשורת.
- תחזית עתידית בדשבורד.
- אזור ניתוח מתקדם.
- קבצי snapshot מקומיים למצב דמו.

## 8. תוצאות נוכחיות מתוך קבצי הדמו

הנתונים הבאים נלקחו מקבצי `output/run.json` ו-`output/ml_results.json` לאחר הרצת pipeline במצב dry-run.

- מספר כתבות: **141**
- מספר שווקים: **25**
- חלון נתונים בהרצה: **30 ימים**  
  הערה: ברירת המחדל בקוד תומכת ב-90 ימים, אך קובץ `.env` מקומי יכול לעקוף זאת.
- Pearson r: **0.145**
- p-value: **0.5787**
- מובהקות סטטיסטית: **לא**; p-value גבוה מ-0.05.

### תוצאות ML — יעד שעה קדימה

- Random Forest MAE: **0.008368**
- Baseline MAE: **0.005833**
- שיפור מול baseline: **-43.464%**
- R²: **-0.215188**
- דיוק כיווני: **70.19%**
- תווית אמינות: **נמוכה**

### תוצאות ML — יעד יום קדימה

- Random Forest MAE: **0.030697**
- Baseline MAE: **0.024066**
- שיפור מול baseline: **-27.553%**
- R²: **-0.615858**
- דיוק כיווני: **48.54%**
- תווית אמינות: **נמוכה**

מקורות תקשורת שנכללו בדמו הנוכחי כוללים: BBC News, The New York Times, The Jerusalem Post, Ynet, The Times of Israel, Fox News, Maariv ו-Globes.

## 9. פרשנות אקדמית

נמצאה אינדיקציה לקשר, אך לא התקבלה מובהקות סטטיסטית מלאה במדגם הנוכחי.

מודל ה-Random Forest הנוכחי אינו משפר את תחזית הבסיס במדדי MAE. כוח ההסבר של המודל עדיין מוגבל, כפי שמשתקף בערכי R² שליליים בהרצת הדמו. לכן אין לטעון שהמודל מספק חיזוי חזק או סיבתי.

הדיוק הכיווני לשעה קדימה גבוה יחסית בדמו הנוכחי, אך מכיוון שהמודל אינו משפר את baseline ב-MAE, יש להתייחס לכך כאינדיקציה ראשונית בלבד ולא כמסקנה חזקה.

לא בוצעה בדיקת Granger causality או בדיקה סיבתית אחרת. לכן אין לטעון לסיבתיות בין סיקור חדשותי לבין תנועות השוק.

## 10. מגבלות

- RSS feeds מספקים לעיתים רק כתבות מהתקופה האחרונה, ולכן אין היסטוריה חדשותית מלאה ל-90 ימים.
- חלק ממקורות החדשות עשויים לחסום בקשות RSS או להחזיר שגיאות זמניות.
- מודל הסנטימנט עשוי לא להבין באופן מושלם הקשרים פוליטיים, אירוניה, ניסוחים ביטחוניים או מסגור תקשורתי.
- תנועות בשוקי Polymarket מושפעות מגורמים רבים שאינם מופיעים בכתבות: נזילות, פעילות סוחרים, שמועות, אירועים בזמן אמת וגורמי מאקרו.
- המודל חוזה תנועת הסתברות בשוק, לא אמת עובדתית ולא תוצאה בעולם האמיתי.
- ציון השפעה של מקור תקשורת אינו מודד אמת או אמינות עיתונאית, אלא עד כמה מאפייני המקור תרמו לחיזוי תנועות בפולימרקט.

## 11. איך להריץ מקומית

```bash
cd Polymarket_Geopolitics
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python -m pipeline.run_pipeline --dry-run
streamlit run frontend/app.py
```

אם קיימים כבר קבצי `output/*.json`, ניתן להריץ את הדשבורד ישירות:

```bash
streamlit run frontend/app.py
```

## 12. מצב דמו

הדשבורד תומך במצב דמו ללא Firebase. כאשר אין הרשאות Firebase, המערכת טוענת את קבצי ה-JSON המקומיים מתוך `output/` ומציגה בדשבורד:

**מצב דמו — נתונים מקומיים**

מצב זה מיועד לבדיקת מורה ולהצגת אב-טיפוס עובד ללא צורך בסיסמאות או מפתחות שירות.

## 13. המלצת פריסה

הדרך המומלצת לפריסת אב-הטיפוס היא:

1. העלאה ידנית של הפרויקט ל-GitHub.
2. יצירת אפליקציה ב-Streamlit Community Cloud.
3. בחירת קובץ כניסה: `frontend/app.py`.
4. שימוש ב-`requirements.txt` להתקנת תלויות.
5. השארת קבצי `output/*.json` לצורך דמו ללא Firebase.
6. לא להעלות `.env` או `firebase_creds.json`.

אין צורך לפרוס את Firebase לצורך בדיקת הדמו.
